import styled from "@emotion/styled";
import { FontSize } from "../../layout/ColorScale";

// Title
export const PageTitle = styled.div`
  padding: 3rem 0;
  font-size: ${FontSize.sub_title};
  font-weight: 700;
`;
